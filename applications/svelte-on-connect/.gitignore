mise.toml

node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# The repo root .gitignore has a Python `lib/` rule that also matches
# SvelteKit's src/lib. Re-include it so our components/models are tracked.
!src/lib/
!src/lib/**
